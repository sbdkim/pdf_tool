export function HeroPanel() {
  return (
    <section className="workspace-hero">
      <div className="hero-card">
        <p className="eyebrow">Workspace</p>
        <h1>Professional PDF cleanup without uploading your documents.</h1>
        <p className="lead">
          Merge multiple files, rotate pages, delete noise, reorder the final sequence, and export
          with cleaner filenames from one focused workspace.
        </p>
      </div>
      <aside className="hero-aside">
        <p className="eyebrow">Launch promise</p>
        <h2>Local. Focused. Reliable.</h2>
        <p className="muted">
          PDF Foundry launch v1 keeps the feature set narrow so the core flows feel polished
          instead of rushed.
        </p>
      </aside>
    </section>
  );
}
